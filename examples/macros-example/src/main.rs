fn main() {
    println!("MCPR Macros Example");
    println!("To run the test, use: ./test_stdio.sh");

    // You can also programmatically run the test here if needed
    // let status = std::process::Command::new("./test_stdio.sh")
    //     .current_dir("examples/macros-example")
    //     .status()
    //     .expect("Failed to execute test script");
    //
    // if !status.success() {
    //     std::process::exit(status.code().unwrap_or(1));
    // }
}
